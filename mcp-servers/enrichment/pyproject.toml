[project]
name = "channel-tools-enrichment"
version = "0.1.0"
description = "MCP server for contact and company data enrichment"
requires-python = ">=3.11"
dependencies = ["mcp>=1.0.0", "httpx>=0.27.0"]

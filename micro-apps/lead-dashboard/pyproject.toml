[project]
name = "lead-dashboard"
version = "0.1.0"
description = "Lead Gen Dashboard - Web UI for company search, enrichment, and lead scoring"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "httpx>=0.27.0",
    "jinja2>=3.1.0",
]